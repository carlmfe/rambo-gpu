@PACKAGE_INIT@

# =============================================================================
# RAMBO Kokkos Library - CMake Package Configuration
# =============================================================================
# This file is used by find_package(rambo-kokkos) to locate and configure
# the RAMBO library for use in other CMake projects.
#
# Usage:
#   find_package(rambo-kokkos REQUIRED)
#   target_link_libraries(my_target PRIVATE rambo::kokkos)
#
# This will automatically:
#   - Add the RAMBO include directories
#   - Link against Kokkos::kokkos
#   - Set required C++ standard (C++17)
# =============================================================================

include(CMakeFindDependencyMacro)

# Find required dependencies
find_dependency(Kokkos REQUIRED)

# Include targets
include("${CMAKE_CURRENT_LIST_DIR}/rambo-kokkos-targets.cmake")

# Check that all required components are available
check_required_components(rambo-kokkos)
